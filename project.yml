#
# Xcode project definition — the .xcodeproj is generated from this and is not
# checked in. Regenerate with `Scripts/generate.sh` (or `xcodegen generate`)
# after adding or moving files.
#
name: PixelPilot

options:
  bundleIdPrefix: dev.rb
  createIntermediateGroups: true
  deploymentTarget:
    macOS: "26.0"

packages:
  PixelPilotCore:
    path: Packages/PixelPilotCore

targets:
  PixelPilot:
    type: application
    platform: macOS

    sources:
      - path: Sources/PixelPilot
        excludes:
          - "Support/*.plist"
          - "Support/*.entitlements"

    dependencies:
      - package: PixelPilotCore
        product: PixelPilotCore

    info:
      path: Sources/PixelPilot/Support/Info.plist
      properties:
        # No Dock icon and no menu bar: this lives in the status bar.
        LSUIElement: true
        CFBundleDisplayName: Pixel Pilot
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        NSHumanReadableCopyright: ""
        # Only reached if someone adds a sunrise or sunset stop to the
        # schedule, and only once — the answer is rounded to one decimal place
        # and stored, and the location manager is let go immediately after.
        # Clock stops need none of this, and there is a manual entry field so
        # solar stops do not require the grant either.
        NSLocationWhenInUseUsageDescription:
          Used once to work out when the sun rises and sets where you are, so the schedule
          can follow it. Stored rounded to about 11 km, and never sent anywhere.
        LSMinimumSystemVersion: $(MACOSX_DEPLOYMENT_TARGET)

    entitlements:
      path: Sources/PixelPilot/Support/PixelPilot.entitlements
      properties:
        # The sandbox is incompatible with what this app does: raw IOKit access
        # for DDC, a system-wide event tap for the media keys, and gamma table
        # writes. None of those have sandbox entitlements.
        com.apple.security.app-sandbox: false

    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: dev.rb.pixelpilot
        PRODUCT_NAME: Pixel Pilot
        # The product name has a space, and the module name is derived from it —
        # which would make the module "Pixel_Pilot" and unimportable from tests.
        PRODUCT_MODULE_NAME: PixelPilot
        MARKETING_VERSION: "0.1.0"
        CURRENT_PROJECT_VERSION: "1"

        SWIFT_VERSION: "6.0"
        SWIFT_STRICT_CONCURRENCY: complete
        SWIFT_UPCOMING_FEATURE_EXISTENTIAL_ANY: YES

        # Automatic signing is what makes Xcode actually issue a development
        # certificate. Signing in alone does not: Xcode only requests one when a
        # target asks for it, so the previous manual ad-hoc setup left the
        # account registered and the certificate list empty.
        #
        # A stable signature is what lets macOS remember the Accessibility grant
        # across rebuilds, and what SMAppService requires for "Open at login".
        #
        # DEVELOPMENT_TEAM is filled in below once the team has been selected
        # once in Xcode. It must live here rather than in the project, because
        # Scripts/generate.sh rewrites the project from this file.
        #
        # CODE_SIGN_IDENTITY must be stated explicitly: XcodeGen defaults macOS
        # targets to "-" (sign to run locally), and with that set Xcode sees no
        # need for a certificate no matter which team is selected.
        CODE_SIGN_STYLE: Automatic
        CODE_SIGN_IDENTITY: "Apple Development"
        DEVELOPMENT_TEAM: UA98Q7K4JR
        CODE_SIGNING_REQUIRED: YES
        CODE_SIGNING_ALLOWED: YES
        ENABLE_HARDENED_RUNTIME: YES

        DEAD_CODE_STRIPPING: YES
        ENABLE_USER_SCRIPT_SANDBOXING: NO
        GENERATE_INFOPLIST_FILE: NO
        COMBINE_HIDPI_IMAGES: YES

        # Assets are generated by Scripts/make-icons.swift rather than drawn by
        # hand, so the artwork is reproducible and every size is rendered at its
        # target resolution instead of downscaled.
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon

      configs:
        Debug:
          SWIFT_ACTIVE_COMPILATION_CONDITIONS: DEBUG
          ONLY_ACTIVE_ARCH: YES
        Release:
          SWIFT_COMPILATION_MODE: wholemodule

  # The core package carries most of the tests, but the reconnect and
  # disappearance paths live in AppModel and cannot be reached from there.
  PixelPilotTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - path: Tests/PixelPilotTests
    dependencies:
      - target: PixelPilot
      - package: PixelPilotCore
        product: PixelPilotCore
    settings:
      base:
        SWIFT_VERSION: "6.0"
        SWIFT_STRICT_CONCURRENCY: complete
        GENERATE_INFOPLIST_FILE: YES
        BUNDLE_LOADER: $(TEST_HOST)
        TEST_HOST: $(BUILT_PRODUCTS_DIR)/Pixel Pilot.app/Contents/MacOS/Pixel Pilot
        # Must match the host app's team. A test bundle signed by anyone else is
        # refused at load time — "mapping process and mapped file have different
        # Team IDs" — because it is dlopen'd into the signed app.
        CODE_SIGN_STYLE: Automatic
        CODE_SIGN_IDENTITY: "Apple Development"
        DEVELOPMENT_TEAM: UA98Q7K4JR

schemes:
  PixelPilot:
    build:
      targets:
        PixelPilot: all
        PixelPilotTests: [test]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - PixelPilotTests
    archive:
      config: Release
